import React from'react'


const ScoreQcm = ({count}) => {
        return (
            <div>
                <p>Score:<span style={{color:'#ff8300'}}> { count }</span></p>    
            </div>
        )
    }

export default ScoreQcm
